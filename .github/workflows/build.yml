name: OS Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: ilammy/setup-nasm@v1.4.0
    - uses: actions/checkout@v3
    - name: make
      run: make
